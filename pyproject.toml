[project]
name = "pyXLMS-manuscript"
version = "0.1.0"
description = "Results and code for the pyXLMS manuscript."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "biopandas>=0.5.1",
    "jupyterlab>=4.4.10",
    "pyxlms>=1.7.3",
]
